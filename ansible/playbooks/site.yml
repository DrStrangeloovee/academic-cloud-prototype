---
- name: Configure attackers (baseline + lab NAT)
  hosts: attackers
  become: true
  roles:
    - common
    - attacker_lab_router

- name: Configure targets (baseline + juice shop)
  hosts: targets
  become: true
  roles:
    - common
    - target_juice_shop
